make install        # install all dependencies
make prep           # preprocess COMI-LINGUA
make train          # fine-tune mBERT/XLM-R
make evaluate       # evaluate on test set
make test           # run alignment unit tests
make clean          # wipe generated files
